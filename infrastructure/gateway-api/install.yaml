# Gateway API v1.5.0 Standard CRDs
#
# These CRDs are a cluster-level prerequisite applied via:
#   make gateway-api-crds
#
# The actual CRDs are fetched from the official release:
#   https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.5.0/standard-install.yaml
#
# This placeholder ensures the infrastructure/gateway-api/ kustomization is valid.
# The CRDs must be present before the agentgateway HelmRelease can reconcile.
