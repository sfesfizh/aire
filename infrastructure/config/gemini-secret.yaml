# PLACEHOLDER — do NOT commit real API keys.
# Run `make secrets` to create the actual secret from $GEMINI_API_KEY.
apiVersion: v1
kind: Secret
metadata:
  name: google-secret
  namespace: agentgateway-system
type: Opaque
stringData:
  Authorization: "REPLACE_ME"
